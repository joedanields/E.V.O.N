# ===========================================================
#  E.V.O.N. Backend - Python Dependencies
# ===========================================================

# -- Core Framework ----------------------------------------
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-multipart==0.0.18
httpx==0.28.*

# -- Database ----------------------------------------------
sqlalchemy[asyncio]==2.0.*
aiosqlite==0.20.*

# -- Configuration -----------------------------------------
pydantic==2.10.*
pydantic-settings==2.7.*

# -- Speech-to-Text (Whisper) ------------------------------
faster-whisper==1.1.*
soundfile==0.13.*
numpy>=1.26,<2.1
resampy==0.4.*

# -- LLM (Ollama client is HTTP-based, no extra lib needed)

# -- Text-to-Speech ----------------------------------------
pyttsx3==2.98

# -- System Monitoring -------------------------------------
psutil==6.1.*

# -- Security (SEC-002 optional auth) ----------------------
# No extra deps needed — uses FastAPI built-in security

# -- CUDA / GPU (install separately via pip + CUDA toolkit)
# ctranslate2          (comes with faster-whisper)
# torch                (only if needed for other models)
